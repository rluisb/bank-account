server:
  port: 8080
  servlet:
    context-path: /api/bank-account

spring:
  application:
    name: bank-account
  data:
    mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      username: admin
      password: admin
      database: bank-account